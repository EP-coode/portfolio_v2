import type { NextPage } from "next";
import Head from "next/head";
import Banner from "../src/components/Banner";
import BottomNav from "../src/components/BottomNav";
import BottomNavAction from "../src/components/BottomNavAction";
import CirclePersonIcon from "../src/icons/CirclePersonIcon";
import ResumeIcon from "../src/icons/ResumeIcon";
import Section from "../src/layout/Section";

const Home: NextPage = () => {
  return (
    <div>
      <Head>
        <title>Create Next App</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <Banner />
      <Section title="About me">CONTENT</Section>
      <Section title="My Projects">CONTENT</Section>
      <Section title="Contact Me">CONTENT</Section>
      <BottomNav>
        <BottomNavAction
          icon={<CirclePersonIcon />}
          label="About Me"
          action={() => {
            console.log("ACTION");
          }}
        />
        <BottomNavAction
          icon={<CirclePersonIcon />}
          label="About Me"
          action={() => {
            console.log("ACTION");
          }}
          isActive={true}
        />
        <BottomNavAction
          icon={<ResumeIcon />}
          label="Resume"
          action={() => {
            console.log("ACTION");
          }}
        />
      </BottomNav>
    </div>
  );
};

export default Home;
