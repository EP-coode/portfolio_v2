import React from "react";

type Props = {
  label: string;
  isActive: boolean;
  action: () => void;
};

const SideNavAction = (props: Props) => {
  return <div>SideNavAction</div>;
};

export default SideNavAction;
